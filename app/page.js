import React from "react";

const HomePage = () => {
  return (
    <div>
      <h1>HomePage</h1>
      <p>
        It works seamlessly with custom Tailwind configurations, and because
        it’s just a Prettier plugin, it works anywhere Prettier works —
        including every popular editor and IDE, and of course on the command
        line.
      </p>
    </div>
  );
};

export default HomePage;
